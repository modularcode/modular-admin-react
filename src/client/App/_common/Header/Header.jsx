// Libs
import React, { Component } from 'react';

// Components
export default class Header extends Component {
  render() {
    return (
      <header>
        The Header
      </header>
    );
  }
}
